<div class= "container px-5" >
  <div class= "row">
    <!--userinfo/投稿フォーム-->
    <%= render partial: 'books/user_post', locals: { user: @user, book: @book } %>

    <!--投稿一覧-->
    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
        <%= render 'index', books: @books %>
    </div>
  </div>
</div>